include:
  - remote: 'https://gitlab.com/cscs-ci/recipes/-/raw/master/templates/v2/.ci-ext.yml'

stages:
  - build
  - test

variables:
  IMAGE_NAME: jfrog.svc.cscs.ch/docker-group-csstaff/alps-images/alps-ngc-pytorch
  PERSIST_IMAGE_NAME: $IMAGE_NAME:25.12-py3

build-alps-ngc-pytorch:
  stage: build
  extends: [.container-builder-cscs-gh200]
  timeout: 6h
  variables:
    DOCKERFILE: Alps-Images/NGC/Pytorch/Containerfile_25.12
    CUSTOM_REGISTRY_USERNAME: $JFROG_USER
    CUSTOM_REGISTRY_PASSWORD: $JFROG_KEY
    #CSCS_REBUILD_POLICY: ALWAYS
    CSCS_REBUILD_POLICY: if-not-exists

test-alps-ngc-pytorch:
  stage: test
  extends: [.container-runner-clariden-gh200]
  image: $PERSIST_IMAGE_NAME
  script:
    - alltoall_perf -b 32 -e 4M -f 2 -g 1
    - |
      wget -O /workspace/config.yaml https://raw.githubusercontent.com/theely/vetnode/refs/heads/main/examples/image-vet/config.yaml
      cd /workspace
      python -m venv --system-site-packages .venv-${SLURM_LOCALID}
      source .venv-${SLURM_LOCALID}/bin/activate
      pip install --no-cache-dir vetnode
      vetnode setup /workspace/config.yaml
    - echo "[image-vet] diagnose"
    - cd /workspace
    - source .venv-${SLURM_LOCALID}/bin/activate
    - vetnode diagnose /workspace/config.yaml
  variables:
    USE_MPI: NO
    SLURM_JOB_NUM_NODES: 2
    SLURM_NTASKS: 8
    SLURM_MPI_TYPE: pmix
    SLURM_NETWORK: disable_rdzv_get
    PMIX_MCA_psec: native
    EDF_APPEND: 'annotations.com.hooks.cxi.enabled = "false"'
